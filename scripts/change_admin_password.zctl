import sys
import transaction

user = app.acl_users.getUserById('admin')
password = sys.argv[1]
encrypted_password = app.acl_users._encryptPassword(password)

user.__ = encrypted_password
transaction.commit()
